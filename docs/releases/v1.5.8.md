## 协议兼容性与可靠性修复

- 修复 Messages 转 Chat / Responses 时的工具参数精度、图片内容、结构化输出与并行工具调用控制。
- 完善 Responses 思考签名回传、多段摘要去重、拒绝状态及流式结束检查。
- 修复 Gemini 工具名称冲突，保留带签名历史；正确传递输出长度和停止序列，并报告安全拦截状态。
- 修复 Command Code 工具参数及 Schema 的数字精度。
- 修复带查询参数、编码路径的上游 URL，以及原生 Anthropic 请求中的本地 `[1m]` 模型后缀。
- 修复全缓存命中时的 token 用量统计。
- 自动更新在替换前校验二进制身份和平台，并保留旧版备份；兼容历史版本标签。
- `preview` 正确返回配置或运行时准备错误。

## 构建与体积优化

- 仅链接实际使用的 o200k 分词器，去除未使用词表，保留原有精确 token 计数。
- 六个平台使用独立构建任务，避免在同一台机器上争抢编译资源。
- 按目标平台缓存依赖，并在主分支依赖或构建设置变化时预热，供后续版本复用。
- 发布前运行竞态检测和静态检查；继续提供六个平台的独立二进制与 npm 包，新增 `SHA256SUMS` 校验文件。
